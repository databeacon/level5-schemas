{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://github.com/databeacon/level5-schemas/schemas/firehoseFLIFO.schema.json",
    "title": "Firehose Positions",
    "version": "1.0.0",
    "description": "Definition for a firehose positions data",
    "type": "object",
    "properties": {
        "clock": {
            "description": "Report time. POSIX epoch format. Time should be the time generated by flight hardware if possible",
            "type": "integer"
        },
        "pitr": {
            "description": "Point in time recovery",
            "type": "integer"
        },
        "id": {
            "description": "FlightAware flight id",
            "type": "string"
        },
        "ident": {
            "description": "Callsign identifying the flight. Typically, ICAO airline code plus IATA/ticketing flight number, or the aircraft registration",
            "type": "string"
        },
        "atcident": {
            "description": "Identifier used for ATC, if it differs from flight identifier",
            "type": "string"
        },
        "lat": {
            "description": "Latitude in decimal degrees, rounded to 5 decimal points",
            "type": "number"
        },
        "lon": {
            "description": "Longitude in decimal degrees, rounded to 5 decimal points",
            "type": "number"
        },
        "alt": {
            "description": "Altitude in feet (MSL)",
            "type": "number"
        },
        "nav_altitude": {
            "description": "The altitude setting, in feet, from the navigation equipment (Mode-S)",
            "type": "number"
        },
        "nav_heading": {
            "description": "The heading, in degrees, from the navigation equipment (Mode-S)",
            "type": "number"
        },
        "heading": {
            "description": "Course in degrees",
            "type": "integer"
        },
        "vertRate": {
            "description": "The aircraft's vertical rate of climb/descent derived from pressure altitude, reported in feet per minute. (version 21.0+)",
            "type": "integer"
        }
    },
    "required": [ "clock", "id", "pitr", "ident", "lat", "lon" ]
}